# 📚 Документация Healthy Eating

## Обзор проекта

**Healthy Eating** — полнофункциональное мобильное приложение для планирования и отслеживания здорового питания на базе Flutter + Spring Boot.

### Стек технологий

| Слой | Технология |
|------|-----------|
| **Мобильный клиент** | Flutter 3.9.2, Dart |
| **Backend** | Spring Boot 3.0, Java 21 |
| **БД** | PostgreSQL 14+ |
| **Коммуникация** | REST API (HTTP), WebSocket (STOMP) |
| **Уведомления** | Flutter Local Notifications |

---

## 1️⃣ Протоколы обмена данными

### REST API
✅ **Реализовано**

Приложение использует REST API для взаимодействия с сервером.

**Примеры запросов:**

```bash
# Получить все рецепты
GET http://172.20.10.5:8080/api/recipes

# Получить рецепт по ID
GET http://172.20.10.5:8080/api/recipes/1

# Авторизация
POST http://172.20.10.5:8080/api/users/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "SecurePass123"
}
```

**Обработка ответов:**

- Успешные ответы: HTTP 200-201
- Ошибки клиента: HTTP 400, 401, 422
- Ошибки сервера: HTTP 500

```dart
// Пример обработки в коде
try {
  final response = await http.get(url).timeout(Duration(seconds: 15));
  if (response.statusCode == 200) {
    // Обработка успеха
  } else if (response.statusCode == 404) {
    throw Exception('Ресурс не найден');
  }
} on TimeoutException {
  throw Exception('Таймаут подключения');
} on SocketException {
  throw Exception('Ошибка сети');
}
```

### WebSocket (Real-time уведомления)
✅ **Реализовано**

Backend поддерживает WebSocket через STOMP для real-time обновлений.

**Подключение:**

```dart
final wsUrl = 'ws://172.20.10.5:8080/ws';
final client = StompClient(
  config: StompConfig(
    url: wsUrl,
    onConnect: (frame) {
      client.subscribe(
        destination: '/topic/updates',
        callback: (frame) {
          // Обработка сообщения
        },
      );
    },
  ),
);
client.activate();
```

**События:**

- `/topic/updates` — общие уведомления
- Поддерживается автоматическое переподключение

### Локальные уведомления
✅ **Реализовано**

Flutter Local Notifications для отправки уведомлений на устройство.

**Примеры:**

```dart
// При загрузке рецептов
LocalNotificationsService.instance.notifyRecipeLoaded(50);

// При сохранении плана
LocalNotificationsService.instance.notifyPlanSaved();

// При достижении цели
LocalNotificationsService.instance.notifyGoalReached();
```

---

## 2️⃣ Архитектура приложения

### Backend (Spring Boot)

```
src/main/java/com/example/healthyeating/
├── controller/
│   ├── RecipeController.java       # GET /api/recipes
│   ├── UserController.java         # POST /api/users/login
│   └── IngredientController.java   # GET /api/ingredients
├── service/
│   ├── RecipeService.java          # Бизнес-логика
│   ├── UserService.java
│   └── IngredientService.java
├── repository/
│   ├── RecipeRepository.java       # JPA Repository
│   ├── UserRepository.java
│   └── IngredientRepository.java
└── entity/
    ├── Recipe.java                 # @Entity для БД
    ├── User.java
    └── Ingredient.java
```

**Особенности:**

- ✅ MVC архитектура
- ✅ JPA для работы с БД
- ✅ Spring Security для авторизации
- ✅ Validation (аннотации @Valid, @NotBlank)
- ✅ Exception Handling через @ControllerAdvice

### Frontend (Flutter)

```
lib/
├── services/
│   ├── api_service.dart            # REST запросы + retry логика
│   ├── websocket_service.dart      # WebSocket подключение
│   ├── notification_service.dart   # Snackbar уведомления
│   └── local_notifications_service.dart  # Локальные уведомления
├── screens/
│   ├── login_screen.dart           # Авторизация
│   ├── home_screen.dart            # Главное окно
│   ├── catalog_screen.dart         # Список рецептов
│   ├── planner_screen.dart         # Планировщик питания
│   └── profile_screen.dart         # Профиль
├── models/
│   ├── recipe.dart                 # Модель рецепта
│   └── nutrients.dart              # Питательные вещества
└── widgets/
    └── recipe_card.dart            # UI компонент
```

**Особенности:**

- ✅ StatefulWidget + setState для управления состоянием
- ✅ FutureBuilder для асинхронной загрузки
- ✅ SharedPreferences для локального хранилища
- ✅ Retry логика в ApiService (3 попытки с backoff)
- ✅ Graceful error handling

---

## 3️⃣ Ключевые функции

### 🔐 Авторизация

**Регистрация + вход:**

```dart
// Регистрация
final result = await ApiService.register(
  'user@example.com',
  'John Doe',
  'SecurePass123'
);
// Сохраняется токен в SharedPreferences

// Вход
final result = await ApiService.login(
  'user@example.com',
  'SecurePass123'
);
```

**Валидация:**

- Email: формат (regex)
- Пароль: минимум 8 символов + цифра
- Имя: минимум 2 символа

### 🍽️ Планировщик питания

**Функции:**

- Выбор блюд на завтрак, обед, ужин
- Расчёт суммарных калорий и макронутриентов
- Сохранение плана в локальное хранилище
- История питания (JSON в SharedPreferences)

**Пример сохранённого плана:**

```json
{
  "breakfast": 1,
  "lunch": 2,
  "dinner": 3
}
```

### 📊 История и прогресс

**Что отслеживается:**

- Дата
- Выбранные блюда
- Итоговые калории/макронутриенты
- Прогресс к дневной цели

**Хранилище:**

```json
[
  {
    "date": "2024-01-15T19:30:00",
    "breakfast": 1,
    "lunch": 2,
    "dinner": 3,
    "totals": {
      "calories": 2150,
      "proteins": 80.5,
      "fats": 45.2,
      "carbs": 250.0
    }
  }
]
```

---

## 4️⃣ Тестирование

### Unit тесты

```bash
flutter test test/api_service_test.dart
```

**Покрытие:**

- ✅ Валидация email/пароля
- ✅ Обработка пустых значений
- ✅ Fallback для отсутствующих данных
- ✅ Логирование запросов

### Widget тесты

```bash
flutter test test/widget_test.dart
```

**Покрытие:**

- ✅ Отображение экранов
- ✅ Навигация
- ✅ Валидация форм

### Запуск всех тестов

```bash
flutter test --coverage
```

---

## 5️⃣ Безопасность

### Обработка ошибок

```
┌─────────────────────┐
│  API Request        │
└──────────┬──────────┘
           │
           ├─→ Попытка 1 (timeout) → Ошибка, повтор через 2 сек
           │
           ├─→ Попытка 2 (успех) → Обработка ответа
           │
           └─→ Попытка 3 (если нужна)
```

**Retry логика:**

- Максимум 3 попытки
- Exponential backoff (2s, 4s, 6s)
- Timeout: 15 секунд

### Валидация данных

```dart
// На клиенте
if (email.isEmpty || !email.contains('@')) {
  throw Exception('Некорректный email');
}

// На сервере
@Valid @NotBlank String email;
```

---

## 6️⃣ Развёртывание

### Локальное тестирование

```bash
# Flutter
flutter run

# Spring Boot
mvn spring-boot:run
```

### Конфигурация IP

Отредактируйте `lib/services/api_service.dart`:

```dart
static const String baseUrl = "http://172.20.10.5:8080";
```

Замените IP на реальный адрес сервера.

---

## 7️⃣ Логирование

### Просмотр логов

```dart
final logs = ApiService.getRequestLog();
for (var log in logs) {
  print(log);
}
```

**Пример вывода:**

```
[2024-01-15T19:30:00.123] 📥 Загрузка рецептов
[2024-01-15T19:30:00.234] Попытка запроса 1/3
[2024-01-15T19:30:01.456] Запрос успешен: 200
[2024-01-15T19:30:01.567] ✅ Загружено 50 рецептов
```

---

## 📞 Контакты

- **Email:** support@healthyeating.app
- **API Docs:** `http://172.20.10.5:8080/swagger-ui.html`
- **GitHub:** https://github.com/yourrepo

---

**Версия:** 1.0.0  
**Последнее обновление:** 15 января 2024
